type Query {
  jobs: [Job!] # each job cannot be null
  job(id: ID!): Job
  company(id: ID!): Company
}

type Job {
  id: ID!
  title: String!
  company: Company
  description: String
}

type Company {
  id: ID!
  name: String!
  description: String
  jobs: [Job!]
}
